- name: Ping Supabase
  run: |
    curl -i "https://cfikksowpygcqyvsldup.supabase.co/rest/v1/broadcasts?select=id&limit=1" \
    -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
    -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}"
